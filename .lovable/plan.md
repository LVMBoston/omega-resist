

# Update Test Protocol Document

## Change

Update `docs/PLAN_DataTemplateEditor_Refactor_TestProtocol.md` to reflect the current state: map hotspot testing is not needed (no working map template exists), and the two existing baselines (Samizdat Template-1 for image mode, World-Map for solid-color mode) are sufficient for the refactor verification.

No structural changes to the document -- just minor clarifications:

1. Add a note in Section 2 (Reference Templates) that map hotspot rendering is unaffected by the layout refactor and does not require a dedicated baseline.
2. No other changes needed -- the document already accurately reflects the captured baselines and verification steps.

## File

- `docs/PLAN_DataTemplateEditor_Refactor_TestProtocol.md` (edit)

