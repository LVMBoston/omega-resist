Add the previously discussed PWA (Progressive Web App) fullscreen implementation to the project backlog as a pending item for future review.

Steps:
1. Read `docs/BACKLOG.md` to locate the Pending / Future section.
2. Append a new entry describing the PWA plan: enable installability via manifest + iOS meta tags, support standalone fullscreen mode, and an "Add to Home Screen" hint for iOS users.
3. Save the updated backlog file.

No code changes to the app itself.